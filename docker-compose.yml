version: "3"
   
services:
  goserver:
    container_name: goserver
    build:
      context: ./goserver
    ports:
      - "8089:8089"
    volumes:
      - "./goserver:/goserver"
    networks:
      - go_network
networks:
  go_network:
    driver: bridge